import { Container } from 'react-bootstrap';

export default function About() {
  return (
    <Container className="mt-4">
      <h1>About</h1>
      <p>
        This project was scaffolded with Electron Forge + Vite and uses
        React with TypeScript and React Router for navigation.
      </p>
    </Container>
  );
}
